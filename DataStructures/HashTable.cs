using System.Collections;

namespace DataStructures
{
	public class HashTable<TKey, TValue>
	{
		public static void CheckUsage(IEnumerable<T>)
		{
			var hashTable = new HashTable<TKey, TValue>();
			
			
		}
	}
}